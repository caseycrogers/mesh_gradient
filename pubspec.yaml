name: mesh_gradient
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  shaders:
    - packages/mesh_gradient/shaders/mesh_gradient.frag
    - packages/mesh_gradient/shaders/dither.frag
    - packages/mesh_gradient/shaders/color_band.frag
    - packages/mesh_gradient/shaders/polar_mask.frag
